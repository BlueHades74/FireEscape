using UnityEngine;

public class Pickup : MonoBehaviour
{
    private int score;
    private bool carryable;
    private bool winCon;
    private bool passable;
    private bool heavy;
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void OnEnable()
    {
        
        
    }

    // Update is called once per frame
    void OnDisable()
    {
        
    }
}
